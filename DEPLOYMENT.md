# Deployment status

## Canonical deployment

This project is awaiting its first canonical GenLayer StudioNet deployment.

- Network: GenLayer StudioNet
- Chain ID: `61999` / `0xf22f`
- Contract: `PENDING`
- Deployment transaction: `PENDING`
- Deployed source commit: `PENDING`
- `contracts/Wearline.py` SHA-256: `PENDING`
- Explorer source/schema parity: `PENDING`
- Hosted frontend: `PENDING`

## Live lifecycle proof to record

After deployment, record finalized evidence for:

1. `ACCEPTED` with all requirements `SATISFIED`.
2. `REMEDIATION_REQUIRED` with a `PARTIALLY_SATISFIED` or `NOT_SATISFIED` requirement.
3. `REVIEW_REQUIRED` with an `INCONCLUSIVE` requirement.
4. Proof correction from revision 1 to revision 2 before verification.
5. A negative integrity proof such as a hash mismatch.

For each case, record the work-order ID, requirement index, transaction hashes, evidence URLs, SHA-256 values, finalized state, verdict/result, and explorer links.
